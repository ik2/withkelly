{% extends "base_external.html" %}

{% block content %}
    <!-- Post ================================================== -->
    <div class="container post">
      <h1>{{ post.subject }}</h1>
      <h2>{{ post.created.strftime("%b %d, %Y") }}</h2>
      {% if post.image %}
      <img src="img?img_id={{ post.key() }}" alt="">
      {% endif %}
      <p>{{ post.content }}</p>
      <hr class="featurette-divider">
    </div><!-- /.post -->
{% endblock %}

