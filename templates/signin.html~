{% extends "base_internal.html" %}

{% block content %}

      <form class="form" method="post">
        <h1 class="center">With Kelly</h1>
        <h2 class="form-heading">Please sign in</h2>
        <input type="text" class="form-control form-top" name="username" value="{{ username }}" placeholder="Username" required autofocus>
        <input type="password" class="form-control form-bottom" name="password" placeholder="Password" required>
        <label class="checkbox">
          <input type="checkbox" value="remember-me"> Remember me
        </label>
        <div style="color: red">{{ error }}</div>
        <button class="btn btn-lg btn-primary btn-block" type="submit">Sign in</button>
        <div class="center"><a href="/signup">Create an account</a><div>
      </form>

{% endblock %}
